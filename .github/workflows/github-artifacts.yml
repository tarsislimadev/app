name: APK on Github Artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'

jobs:
  artifacts:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3

      - name: Create an Ionic project
        run: docker-compose -f ./docker-compose.yaml run app ionic init hello --type=custom --project-id=com.example.hello

      - name: Create an Android project
        run: docker-compose -f ./docker-compose.yaml run app cordova create hello com.example.hello hello

      - name: List files
        run: docker-compose -f ./docker-compose.yaml run --workdir /app/hello app ls -la

      - name: Enable cordova
        run: docker-compose -f ./docker-compose.yaml run --workdir /app/hello app ionic integrations enable cordova --add --confirm --prod --no-interactive --noresources

      # - name: Add Android platform
      #   run: docker-compose -f ./docker-compose.yaml run --workdir /app/hello app ionic cordova platform add android --confirm --prod --no-interactive --noresources

      - name: Build .apk file
        run: docker-compose -f ./docker-compose.yaml run --workdir /app/hello app ionic cordova build android --release --prod --no-interactive --device --confirm --prod --no-interactive --noresources

      # - name: Create release
      #   run: gh release create v$( date +%Y%m%d ) ./src/app/platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk
      #   env:
      #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
