name: APK on Github Artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'

jobs:
  artifacts:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3

      - name: Create an Android project
        run: bash ./env/run-app.sh ionic cordova create hello com.example.hello hello --confirm --prod --no-interactive --noresources

      - name: List files
        run: bash ./env/run-app.sh ls -lR

      # - name: Add Android platform
      #   run: bash ./env/run-app.sh ionic cordova platform add android --confirm --prod --no-interactive --noresources

      # - name: Build .apk file
      #   run: bash ./env/run-app.sh ionic cordova build android --release --prod --no-interactive --device

      # - name: Create release
      #   run: gh release create v$( date +%Y%m%d ) ./src/app/platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk
      #   env:
      #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

